name: Setup
description: Setup CI/CD pipeline
runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: 22

    - name: Install dependencies
      shell: bash
      run: npm ci
